apiVersion: v1
kind: Secret
metadata:
  name: stayngo-secrets
type: Opaque
stringData:
  # Base64 encoding is not required when using stringData.
  # Replace these with real credentials in production!
  SECRET_KEY: "replace_with_flask_secret_key"
  SUPABASE_URL: "replace_with_supabase_url"
  SUPABASE_KEY: "replace_with_supabase_key"
